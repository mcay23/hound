// this is a dummy lib for web-only (non-desktop) builds so that the build does not fail
export function defineMpvVideoElement() {}
export class MpvVideoElement {}
export function createMpvMain() {
  return {};
}
